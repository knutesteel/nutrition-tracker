import{redirect}from'next/navigation';import AppShell from'@/components/AppShell';import SignOut from'@/components/SignOut';import NotificationSettings from'@/components/NotificationSettings';import{createClient}from'@/lib/supabase/server';
export default async function Page(){const s=await createClient();const{data:{user}}=await s.auth.getUser();if(!user)redirect('/login');return <AppShell><div className="top"><div><div className="eyebrow">Settings</div><h1 className="title">Preferences</h1></div></div><NotificationSettings/><div className="card"><div className="row"><span>Signed In</span><span className="tiny">{user.email}</span></div><div className="row"><span>Nutrition Reference</span><span className="tiny">FDA Daily Values</span></div><div className="row"><span>BAC Reminder</span><span className="tiny">30 minutes</span></div><div className="row"><span>Install on iPhone</span><span className="tiny">Share → Add to Home Screen</span></div></div><div className="card"><h3 className="section-title">Important</h3><p className="tiny">Nutrition values are AI estimates and may differ from package labels or measured recipes. BAC tracking does not determine fitness to drive.</p></div><SignOut/></AppShell>}
