import{NextResponse}from'next/server';import{createClient}from'@/lib/supabase/server';import{z}from'zod';import{generateObject}from'ai';
const Body=z.object({description:z.string().trim().min(2).max(1000),portion:z.string().trim().min(1).max(100),consumed_at:z.string().datetime()});
const Analysis=z.object({classification:z.enum(['food','alcohol','food_and_alcohol','non_caloric_beverage']),confidence:z.enum(['high','medium','low']),nutrition:z.object({calories:z.number().nonnegative(),protein_g:z.number().nonnegative(),carbs_g:z.number().nonnegative(),fat_g:z.number().nonnegative(),fiber_g:z.number().nonnegative(),sugar_g:z.number().nonnegative(),added_sugar_g:z.number().nonnegative(),sodium_mg:z.number().nonnegative(),potassium_mg:z.number().nonnegative(),calcium_mg:z.number().nonnegative(),iron_mg:z.number().nonnegative(),magnesium_mg:z.number().nonnegative(),vitamin_a_pct:z.number().nonnegative(),vitamin_b6_pct:z.number().nonnegative(),vitamin_b12_pct:z.number().nonnegative(),vitamin_c_pct:z.number().nonnegative(),vitamin_d_pct:z.number().nonnegative(),vitamin_e_pct:z.number().nonnegative(),vitamin_k_pct:z.number().nonnegative(),folate_pct:z.number().nonnegative()}),alcohol:z.object({present:z.boolean(),beverage_type:z.string().nullable(),quantity:z.number().nonnegative(),serving_oz:z.number().nonnegative(),estimated_abv:z.number().min(0).max(1).nullable(),standard_drinks:z.number().nonnegative(),alcohol_calories:z.number().nonnegative()})});
async function analyze(description:string,portion:string){const{object}=await generateObject({model:'inclusionai/ling-3.0-tiny-free',schema:Analysis,system:'You estimate nutrition for a food and alcohol intake diary. Use the stated portion. Return cautious best estimates based on typical USDA/FDA values. ABV is a decimal fraction. One US standard drink contains 0.6 fl oz pure alcohol. Include alcohol calories in total calories. Daily vitamin fields are percentages of FDA Daily Value. Never add commentary.',prompt:`Description: ${description}\nPortion: ${portion}`});return Analysis.parse(object)}
export async function POST(req:Request){try{const body=Body.parse(await req.json());const s=await createClient();const{data:{user}}=await s.auth.getUser();if(!user)return NextResponse.json({error:'Sign in required'},{status:401});const a=await analyze(body.description,body.portion);const{data:entry,error}=await s.from('intake_entries').insert({user_id:user.id,description:body.description,portion:body.portion,consumed_at:body.consumed_at,entry_type:a.classification}).select().single();if(error)throw error;const{error:nError}=await s.from('nutrition_analyses').insert({entry_id:entry.id,user_id:user.id,...a.nutrition,confidence:a.confidence,analysis_json:a});if(nError){await s.from('intake_entries').delete().eq('id',entry.id);throw nError}if(a.alcohol.present){const{error:alError}=await s.from('alcohol_analyses').insert({entry_id:entry.id,user_id:user.id,beverage_type:a.alcohol.beverage_type,quantity:a.alcohol.quantity,serving_oz:a.alcohol.serving_oz,abv:a.alcohol.estimated_abv,standard_drinks:a.alcohol.standard_drinks,alcohol_calories:a.alcohol.alcohol_calories});if(alError)throw alError;const{error:rError}=await s.from('bac_reminders').insert({user_id:user.id,related_entry_id:entry.id,next_reminder_at:new Date(new Date(body.consumed_at).getTime()+30*60*1000).toISOString(),status:'active'});if(rError)throw rError}return NextResponse.json({...a,entry_id:entry.id})}catch(e){console.error(e);return NextResponse.json({error:e instanceof Error?e.message:'Invalid request'},{status:400})}}
