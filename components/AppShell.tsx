'use client'
import Link from 'next/link'
import { usePathname } from 'next/navigation'
import { useState } from 'react'
import IntakeSheet from './IntakeSheet'
export default function AppShell({children}:{children:React.ReactNode}){ const [open,setOpen]=useState(false); const pathname=usePathname(); const navClass=(href:string)=>pathname===href?'active':''; return <><header className="app-header"><div className="app-header-inner"><Link className="wordmark" href="/"><span className="wordmark-icon">↗</span><span>INTAKE</span></Link><span className="header-tag">Food · Alcohol · BAC</span></div></header><div className="shell">{children}</div><div className="nav"><div className="nav-inner"><Link className={navClass('/')} href="/"><span className="nav-icon">◷</span>Today</Link><Link className={navClass('/history')} href="/history"><span className="nav-icon">▤</span>History</Link><button aria-label="Add intake" className="plus" onClick={()=>setOpen(true)}>+</button><Link className={navClass('/trends')} href="/trends"><span className="nav-icon">↗</span>Trends</Link><Link className={navClass('/settings')} href="/settings"><span className="nav-icon">⚙</span>Settings</Link></div></div>{open&&<IntakeSheet onClose={()=>setOpen(false)}/>}</> }
